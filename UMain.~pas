unit UMain;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  TFMain = class(TForm)
    Memo_plaintext: TMemo;
    Memo_Otlad: TMemo;
    MRez: TMemo;
    procedure FormCreate(Sender: TObject);
  private
    { Private declarations }
  public
    plaintext:string;
  end;

var
  FMain: TFMain;

implementation

{$R *.dfm}

uses Uwork_with_text,UAnalyze_text;

procedure TFMain.FormCreate(Sender: TObject);
begin
  Memo_plaintext.Lines.LoadFromFile('123.asm');
  plaintext:='';
  Reconnect_plaintext;//помен€ть им€            //добавить удаление текста в скобках
  Analyze_text;

end;

end.
